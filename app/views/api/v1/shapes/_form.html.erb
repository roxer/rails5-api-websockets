<%= form_for(api_v1_shape) do |f| %>
  <% if api_v1_shape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_shape.errors.count, "error") %> prohibited this api_v1_shape from being saved:</h2>

      <ul>
      <% api_v1_shape.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label %>
  </div>

  <div class="field">
    <%= f.label :shape_type %>
    <%= f.text_field :shape_type %>
  </div>

  <div class="field">
    <%= f.label :canvas_id %>
    <%= f.text_field :canvas_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
